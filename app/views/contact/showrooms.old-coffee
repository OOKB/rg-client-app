React = require 'react'
{div, p, a, h2, strong, address, ul, li} = require 'reactionary'

Showroom = require './showroom'

# this would then build the lists, by combining rendered individual showroom data.
